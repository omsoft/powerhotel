<% provide(:title, "New hotel") %>
<h1>Create hotel</h1>

    <%= form_for(@hotel, html: { class: "" }) do |f| %>
      <%= render 'shared/error_messages', object: f.object %>

      <%= render 'form', f: f %>
      
      <div class="form-group row">
        <div class="col-sm-10">
          <%= f.submit "Create hotel", class: "btn btn-large btn-primary" %>
        </div>
      </div>

    <% end %>
